class Solution:
    def arrayPairSum(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        a=sorted(nums,reverse=True)
        b=len(nums)
        c=0
        for x1 in range(1,b,2):
            c+=a[x1]
        return c
