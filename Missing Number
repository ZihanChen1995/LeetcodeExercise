class Solution:
    def missingNumber(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        a=len(nums)
        l1=list(range(0,a+1))
        s1=sum(nums)
        s2=sum(l1)
        res=abs(s1-s2)
        return res
